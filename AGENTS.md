# Project Instructions

- Start by reading `docs/OPTIMIZATION_TASKS.md`.
- Always continue from the `当前任务` and `任务状态`.
- Execute tasks in strict order (`T01 -> T30`) unless user explicitly changes priority.
- After finishing each task:
  - Update `docs/OPTIMIZATION_TASKS.md` with new status.
  - Summarize changed files and verification results.
  - Then continue to the next task automatically.

