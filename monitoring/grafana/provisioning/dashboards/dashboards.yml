apiVersion: 1

providers:
  - name: monitor-board
    orgId: 1
    folder: Monitor Board
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
